/**
 *
 */

import type { LocalizationConfig } from "@sdkit/config"

export const localization = {
    network: {
        statusMessages: {
            "429": "Try again later. You can find more information about rate limits at 'https://docs.altered.app/api/rate-limit'."
        }
    }
} satisfies LocalizationConfig
