/**
 * Constants for system preferences
 */

// Thought ID used for storing system preferences
export const SYSTEM_PREFERENCES_THOUGHT_ID = "kv"

// Keys for specific system preferences
export const SYSTEM_PREFERENCE_KEYS = {
    AUTO_GENERATE_ALIASES: "auto-generate-aliases"
} as const
